<% if @pagy.next.present? %>
  <div class="flex place-content-center">
    <%= button_to books_path, params: { page: @pagy.next }, method: :get, "data-turbo-stream": true, class: "content-center container rounded-sm text-center py w-fit mx-2 px-2 btn-outline-secondary py-2 px-4 text-dark" do %>
      <img src="icons/page.svg"/>
      Показати більше
      <img src="icons/page.svg"/>
    <% end %>
  </div>
<% end %>