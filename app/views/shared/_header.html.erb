<nav class="d-flex justify-content-between my-3 mx-4">
  <div>
    <a class="navbar-brand" href="/">
        <img src="../logo.svg" alt="Логотип"/>
    </a>
  </div>
  <div>
    <a class="btn mr-2" href="#">Книги</a>
    <a class="btn mr-2" href="#">Як це працює?</a>
    <a class="btn mr-2" href="#">Про нас</a>
    <a class="btn mr-2" href="#">Контакти</a>
  </div>
  <div>
    <form>
      <% if user_signed_in? %> 
        <div> Welcome <%= current_user.name %> </div> 
        <%= button_to "Вийти", destroy_user_session_path, method: :delete, class: "btn mr-2"%> 
        <%= button_to "Видати книгу", root_path, class: "btn bg-gromada-green"%>
      <% else %>
        <%= button_to "Увійти", new_user_session_path, class: "btn mr-2" %> 
      <% end %>
    </form>
  </div>
</nav>
