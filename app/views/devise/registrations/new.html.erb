<div class="flex flex-wrap place-content-center"><h1 class="text-5xl font-extrabold">Зареєструватись</h1></div>

<div class="flex flex-wrap place-content-center mb-4" data-turbo="false">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <div style="width: 500px;">
    <span class="text-lg text-gromada-black">Електронна пошта</span><br/>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control w-full" %>
  </div>

  <div>
    <span class="text-lg text-gromada-black">Пароль</span><br/>
    <%= f.password_field :password, autocomplete: "new-password", class: "form-control w-full mb-2"%>
  </div>

  <div>
    <span class="text-lg text-gromada-black">Підтвердіть пароль</span><br/>
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control w-full mb-2"%>
  </div>

  <div class="flex flex-wrap place-content-center">
    <div class="my-3">
      <%= f.submit "Зареєструватися", class: "border-0 text-xl fw-bold bg-gromada-light-green text-gromada-green py-2 px-5" %>
    </div>
  </div>
  <div class="flex flex-wrap place-content-center">
    <span>Вже маєте акаунт? <%= link_to "Увійти", new_user_session_path, class: "text-gromada-green" %></span>
  </div>
<% end %>
</div>
