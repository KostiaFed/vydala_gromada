<div class="flex flex-wrap place-content-center"><h1 class="text-5xl font-extrabold">Відновити пароль</h1></div>

<div class="flex flex-wrap place-content-center mb-4" data-turbo="false">
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div style="width: 500px;">
    <span class="text-lg text-gromada-black">Електронна пошта</span><br/>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control w-full" %>
  </div>

  <div class="flex flex-wrap place-content-center">
    <div class="my-3">
      <%= f.submit "Надіслати інструкцію з відновлення паролю", class: "border-0 text-xl fw-bold bg-gromada-light-green text-gromada-green py-2 px-5" %>
    </div>
  </div>
  <%= render "devise/shared/links" %>
<% end %>
</div>
